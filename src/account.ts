export class Account {
    // username: string;
    // user: string[];
    // repos: string[];

    repo: string;
  
    
    constructor(public username: string, public user: string, public followers: any, public repos: string, public following: string, public created_at: string){

    }
}
